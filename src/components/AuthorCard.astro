---
import { Image } from 'astro:assets';


interface Props {
    name: string;
    avatar: string;
    twitter: string;
    linkedin: string;
    github: string;
    bio: string;
    subtitle: string;
    slug: string;
}

const { name, avatar, slug, twitter, linkedin, github, bio, subtitle } = Astro.props;
console.log("🚀 ~ slug:", slug)
---

<div class="max-w-lg mx-auto my-10 bg-white rounded-lg shadow-md p-5">
    <div class="flex justify-center">
        <Image
        class="w-32 h-32 rounded-full mx-auto"
        src={avatar}
        alt={`${name}'s avatar`}
        width={200}
        height={200}
        loading="lazy"
        transition:name={`foto-perfil-${slug}`}
        />
    </div>
    <h2 
        class="text-center text-2xl font-semibold mt-3"
        transition:name={`nombre-${slug}`}
    >{name}</h2>
    <p class="text-center text-gray-600 mt-1">{subtitle}</p>
    <div class="flex justify-center mt-5">
        <a href={twitter} class="text-blue-500 hover:text-blue-700 mx-3">Twitter</a>
        <a href={linkedin} class="text-blue-500 hover:text-blue-700 mx-3">LinkedIn</a>
        <a href={github} class="text-blue-500 hover:text-blue-700 mx-3">GitHub</a>
    </div>
    <div class="mt-5">
        <h3 class="text-xl font-semibold">Bio</h3>
        <p class="text-gray-600 mt-2">
            {bio}
        </p>
        <div class="flex justify-end">
            <a href={`/blog/${slug}`} class="text-black">Leer más</a>
        </div>
    </div>
</div>